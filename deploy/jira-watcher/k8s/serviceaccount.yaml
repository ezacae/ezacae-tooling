apiVersion: v1
kind: ServiceAccount
metadata:
  name: jira-watcher
  namespace: jira-watcher
  labels:
    app.kubernetes.io/name: jira-watcher
    app.kubernetes.io/component: serviceaccount
# Aucun droit cluster particulier nécessaire :
# le watcher parle uniquement à JIRA (réseau externe) et à l'API Anthropic.
